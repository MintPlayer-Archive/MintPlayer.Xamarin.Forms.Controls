<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MintPlayer.Xamarin.Forms.Controls"
             x:Class="MintPlayer.Xamarin.Forms.Controls.CheckBox">
    <ContentView.BindingContext>
        <controls:CheckBoxVM />
    </ContentView.BindingContext>
    <ContentView.Content>
        <StackLayout Orientation="Horizontal">
            <!--<CheckBox IsChecked="{Binding IsChecked}" HorizontalOptions="Start" VerticalOptions="Start" />-->
            <Label Text="{Binding Text}" HorizontalOptions="StartAndExpand" VerticalOptions="Start" Padding="0,3,0,0">
                <Label.GestureRecognizers>
                    <!--<TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />-->
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </ContentView.Content>
</ContentView>